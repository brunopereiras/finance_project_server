<%= form_with(model: entry_category, local: true) do |form| %>
  <% if entry_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entry_category.errors.count, "error") %> prohibited this entry_category from being saved:</h2>

      <ul>
      <% entry_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :entry_category_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
